source 'https://rubygems.org'

# rvm is being used - curl -sSL https://get.rvm.io | bash -s stable --ruby

gem 'cucumber'
gem 'bundle'
gem 'capybara'
gem 'selenium-webdriver'
gem 'poltergeist'
gem 'wait_until'
gem 'rspec'

#gem 'phantomjs' - loaded through apt-get install

